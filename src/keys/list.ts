import { Column } from "@/components/table/Table";

export const idColumn: Column = {
    field: "id",
    type: "string",
    header: "productPage.id",
};

export const nameColumn: Column = {
    field: "name",
    type: "string",
    header: "productPage.name",
};

